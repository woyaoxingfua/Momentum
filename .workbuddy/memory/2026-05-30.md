# 2026-05-30 工作日志

## Momentum 项目 - 日志系统升级
- 重写 `logger.py`，从基础 logging 升级为全功能日志系统
- 新特性：RotatingFileHandler 自动轮转（10MB×5备份）、按日期命名文件、request_id 追踪、pid/tid/funcName 详细格式、6个便捷日志函数
- CLI 新增 `--log-file` 和 `--log-dir` 参数
- web.py 的 do_GET/do_POST/do_PUT 注入 `request_context()` 和 `log_api_request()`
- storage.py 的 login_user 注入 `log_security_event()`（登录失败记录）
- .env.example 新增 MOMENTUM_LOG_FILE/LOG_DIR/LOG_MAX_BYTES/LOG_BACKUPS 配置项
- .gitignore 新增 logs/ 和 *.log
- 验证通过：8/8 功能检查、pytest 4 passed（失败项均为预存环境问题）

## 日志默认启用文件输出
- 修改 `init_from_env()` 和 `setup_logging()`：默认启用文件日志写入 `logs/` 目录，无需手动配置
- 新增 `MOMENTUM_LOG_FILE=off` / `MOMENTUM_LOG_DIR=off` 禁用机制
- CLI `--log-file`/`--log-dir` 参数说明更新为"覆盖"而非"启用"
- .env.example 日志部分重写，说明默认已启用
