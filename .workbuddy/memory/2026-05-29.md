# 2026-05-29

## Momentum 项目：master 与 feature/services-enhanced-v1 分支合并完成

完成两个分支的完整代码对比与合并，保留两分支所有功能：

### 已合并的核心文件
- `models.py`：新增 TaskRelationType 枚举、TaskRelation/TaskRelationInput dataclass；Task 新增 tags/subtasks/relations 字段
- `storage.py`：采用 v1 的 @contextmanager 模式 + 保留 master 的 password_hash 迁移逻辑；新增 tags/task_relations/batch/heartbeat 等方法
- `agent_app.py`：保留 master 的 guardrails/会话清理/image_base64 + v1 的新工具（tags/batch 操作）
- `context.py`：新增 heartbeat_suggestion 函数
- `cli.py`：edit 命令新增 --tags 参数

### 新增模块（来自 v1）
- `agents/` 目录：agent.py + tools/(task/subtask/relation/weather/heartbeat)_tools.py
- `services/` 目录：weather.py、location.py、heartbeat.py、task_hierarchy.py、notification.py

### web.py 合并
- 保留 master 全部原有端点 + guardrails/chat stream
- 新增 v1 的 tags/heartbeat/weather/location/subtasks/relations/batch API 端点
- task_to_json 新增 tags 字段

### 前端合并
- index.html：新增 heartbeat 区块、心跳配置、tags 编辑
- app.css：新增 heartbeat/tag/checkbox 样式
- app.js：导入 heartbeat 模块、新增 DOM 元素引用
- config.js：saveConfig 增加心跳配置保存
- tasks.js：新增 tags 显示和编辑
- 新增 heartbeat.js、vision-demo.js、vision-guide.js

### 合并策略
- config.py：两分支一致，无需修改
- chat.js：保留 master 的认证逻辑（更安全）
- storage.py update_status：修复 v1 中 old_parent_id 在 with 块外使用的问题

### 后续修复（用户追问"真的完了吗"后发现）
- web/__init__.py：遗漏了3个文件（__init__.py, handlers.py, legacy_server.py），已补写
- web/__init__.py：`from .server import` → `from .legacy_server import`（文件名不匹配）
- legacy_server.py：所有 `.agent_app` / `.models` / `.auth` 等相对导入改为 `..xxx`（从 web/ 子包指回父包）
- handlers.py：`from .web import` → `from .legacy_server import`
- 最终验证：8/8 全部通过（models/config/context/storage/services/agents/web/功能），29个 Python 文件语法检查全过
